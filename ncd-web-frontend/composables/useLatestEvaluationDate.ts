const useLatestEvaluationDate = async (menteeData: Promise<any>) => {
//   return  await menteeData.then((resp) => {
//     let vm: any[] = [];
//     for (var i = 0; i < resp.length; i++) {
//       vm.push(resp);
//     }
//     console.log(vm);

//     const max = vm.reduce((a, b) => {
//       return Math.max(a, b);
//     });

//     return max.moment().format("LL");
//   });
};

export default useLatestEvaluationDate;
